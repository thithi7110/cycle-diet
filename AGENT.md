# AGENT.md

## Project

CYCPLUS DC1のペダリング記録を、消費カロリーと脂肪量の3Dモデルで確認するダイエットログアプリ。

## Working agreements

- 仕様の変更は `specs/` に、重要な技術判断は `docs/adr/` に記録する。
- 既存のUI言語は日本語。新しい画面文言も日本語にする。
- まずブラウザで成立する小さな体験を作り、データ連携は後から差し替えられる境界を保つ。
- 脂肪量は表示上の換算値であり、医学的な体脂肪測定値ではない。
- 外部依存は必要性が明確になるまで増やさない。

## Validation

- `index.html` はローカルファイルとして開いて動作確認する。
- 主要操作は、今日／累積切替、3D回転、カレンダーの日付選択、ログ追加を確認する。
- HTML/JavaScriptのエラーがないことを確認する。

## Current product assumptions

- 1 kcal = 0.125 g 脂肪として、可視化用に概算する。
- 初期データはデモ用。将来的にCYCPLUS DC1の実測データ入力へ置き換える。
